# Folder reserved for notif_dash (https://github.com/.../notif_dash) — not owned
# by this repo. Populated by `install.sh --wezterm`; the committed folder-loader
# in wezterm.lua loads whatever is here (no-op when the folder is absent/empty).
notif_dash/
